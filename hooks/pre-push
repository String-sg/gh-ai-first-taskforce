#!/bin/sh
set -e

BRANCH=$(git symbolic-ref HEAD 2>/dev/null | sed 's|refs/heads/||')
if [ -n "$BRANCH" ] && echo "$BRANCH" | grep -qE '^(main|master)$'; then
  echo "Direct push to '$BRANCH' is not allowed. Open a pull request instead."
  exit 1
fi
